#include<stdio.h>

int main()
{
    int n,c,r=0;
    scanf("%d",&n);
    c=n;
    while(c>0)
    {
        r=r*10;
        r=r+c%10;
        c/=10;
    }
    if(n==r)
    {
        printf("true");
    }
    else
    {
        printf("false");
    }
    return 0;
}
