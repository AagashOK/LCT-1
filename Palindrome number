bool isPalindrome(int x) {
    int t=x;
    long int r=0;
    while(t!=0)
    {
        int l=t%10;
        r=r*10+l;
        t=t/10;
    }
    if(r==x && x>=0)
    {
        return true;
    }
    else{
        return false;
    }
    
}
